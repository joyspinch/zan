// Negative native compile test: this unspecialized template has no emitted
// internal body. Patch must diagnose it rather than branch to text offset zero.
class MissingBody {
    public static void Absent<T, U>() { }
}
class Program {
    static void Main() { MissingBody.Absent(); }
}
